using Common.Entities;
using Diamonds.Api.Controllers;
using Microsoft.AspNetCore.Mvc;
using System.Collections.Generic;
using System.Linq;
using Xunit;

namespace Diamonds.Api.Tests.XUnit.ApiTests
{

    public  class BoardsControllerTests
    {
        [Fact]
        public void can_get_boards()
        {
            //// Arrange
            //var controller = new BoardsController();

            //// Act
            //var response = controller.Get();

            //// Assert
            //var versionResult = (OkObjectResult)response;
            //var boards =((IEnumerable<Board>) ((OkObjectResult)versionResult).Value);

            //Assert.Equal(1, boards.Count());
            //Assert.Equal("77", boards.First().BoardId);
        }
    }
}
